server:
    port: 8080
    address: 0.0.0.0

spring:
    datasource:
        url: jdbc:mysql://localhost:3306/smart_room_iot?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
        username: root
        password: 206512
    jpa:
        hibernate:
            ddl-auto: update
            show-sql: true

logging:
    level:
        com:
            example:
                demo: DEBUG
